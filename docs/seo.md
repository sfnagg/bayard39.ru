# SEO и индексация bayard39.ru

Состояние на 17 сентября 2026 года. Документ описывает, что показывают поисковики,
что с этим уже сделано и как проверять дальше.

## Коротко

Технически сайт в порядке: уникальные заголовки и описания, canonical, микроразметка,
sitemap, ошибок обхода нет. Яндекс держит в поиске все 11 страниц, Google отстаёт.

**Брендовый провал закрыт.** После PR #14 запросы вида «чоп баярд калининград» ушли
с 46–48 места на 1–3 и принесли первый клик. Коммерческие запросы за ту же неделю не
дали ни одного показа, мерить пока нечего.

Упёрлись не в код, а в отсутствие сигналов доверия: **внешних ссылок две, обе
случайные, ИКС ноль, карточки в Яндекс.Бизнесе нет**. По коммерческим запросам это
держит сайт на третьей странице выдачи независимо от качества вёрстки.

## Что показывают поисковики

### Яндекс.Вебмастер

Данные на 17.09.2026.

| Показатель | Значение |
|---|---|
| Страниц в поиске | 11 из 11 (было 6) |
| Исключено | 1 |
| ИКС | 0 |
| Внешних ссылок | 2 (раньше писали «ноль» — эндпоинт просто не смотрели) |
| Ошибок в sitemap | 0 |

Все пять страниц под типы объектов из PR #14 в поиске. `summary` наконец сошёлся
с `search-urls/in-search/samples`: счётчик `searchable_pages_count` отстаёт на
несколько дней, при расхождении верить samples.

Внешние ссылки — `screenshots.wiki/report/26481` (07.02.2026) и
`akwatoria.ru/blagodarim/sotrudnikov/` (21.12.2025). Обе случайные, ни одна не
добыта намеренно, на ИКС не повлияли.

Диагностика держит одну запись:

- `NOT_IN_SPRAV` — организации нет в Яндекс.Бизнесе. Для локальной компании это
  главная потеря: нет показов в Картах и колдунщика в выдаче.

`DOCUMENTS_MISSING_DESCRIPTION` (файлы подтверждения прав без description) ушёл сам,
состояние `ABSENT` — делать ничего не пришлось. Если вернётся: **закрывать эти файлы
в robots.txt нельзя**, Google должен читать свой, иначе слетит подтверждение прав.

Яндекс периодически запрашивает `/compani/policy.html` — страница политики удалена
в `9d7ac67`, ссылок на неё нет, 404 отдаётся корректно, адрес выпадет из индекса сам.
Возвращать страницу решили не надо (решение от 09.09.2026, вместе с отказом от Метрики).

### Запросы и позиции в Яндексе (08–14.09.2026)

Всего 9 показов за неделю и **1 клик** — первый за всё время наблюдений.

| Запрос | Показы | Позиция |
|---|---|---|
| ооо чоо баярд | 2 | 2 |
| чоп баярд калининград | 2 | 2 (клик) |
| л056-00106-39/00022224 (номер лицензии) | 1 | 13 |
| ооо «чоо «баярд» | 1 | 2 |
| чоп баярд | 1 | 3 |
| ооо чоо | 1 | 8 |
| боярд охрана калининград | 1 | 1 |

Что изменилось против прошлой недели (30.08–05.09, 12 показов, 0 кликов):

1. **Бренд вышел в топ-3.** Неделей раньше «баярд охрана» стояло на 46 месте,
   «баярд сайт» — на 48; теперь все брендовые формулировки с «чоп»/«чоо»/«охрана» дают
   1–3 место, и оттуда пришёл первый клик. Яндекс связал сайт со словом «охрана» —
   это сделали пять страниц под типы объектов из PR #14.
2. **Коммерческих запросов в выборке нет вовсе.** «чоп калининград» (было 25),
   «охранное предприятие калининград» (26), «физическая охрана калининград» (31) за
   неделю не дали ни одного показа, так что позицию по ним измерить нечем — не путать
   с падением. Ручная проверка живой выдачи: по «чоп калининград» на первой странице
   ya.ru сайта нет, дальше второй страницы не пройти — Яндекс отдаёт капчу.
3. Выборка крошечная: 9 показов, любой вывод по одному-двум показам — шум.

Прежний диагноз остаётся в силе: нулевой ИКС и отсутствие карточки в Яндекс.Бизнесе
держат коммерческие запросы на третьей странице. Бренд починился вёрсткой, коммерция
так не починится.

### Google Search Console

Данные на 09.09.2026 — 17.09 не перепроверялись, Search Console API не подключён,
смотреть вручную.

4 страницы в индексе, 3 не проиндексированы:

- «Вариант страницы с тегом canonical» — 1. Это был `/index.html`: меню и логотип вели
  на него вместо `/`. Исправлено в PR #13.
- «Обнаружена, не проиндексирована» — 2. Ждут обхода, кодом не лечится.
- «Страница является копией, канонический вариант не выбран» — 0, проверка подтверждена.

Показов 0–4 в день.

## Что сделано

| PR | Что | Зачем |
|---|---|---|
| #12 | Контекстные ссылки на непросканированные страницы | Поднять их в очереди обхода Google |
| #13 | Все ссылки на главную ведут на `/`, а не на `/index.html` | Убрать второй адрес главной из индекса |
| #14 | Пять страниц под типы объектов, «ЧОП», область, подписи к письмам | Забрать длинный хвост, где выдача пустая |

Подробнее по #14:

- Новые страницы: `ohrana-ofisa`, `ohrana-magazina`, `ohrana-sklada`, `ohrana-stroyki`,
  `ohrana-meropriyatiy`. Одна страница услуг конкурировала сразу за все запросы и не
  выигрывала ни одного.
- `services.html` стал хабом со ссылками на все пять.
- Слово «ЧОП» добавлено рядом с «ЧОО» — ищут именно так, но юридически верное
  «ЧОО» осталось.
- Под 12 благодарственными письмами появились видимые подписи. Раньше текст был
  только в `alt`, для поисковика письма были просто картинками.

## Правила для текстов

Тексты страниц описывают реальную компанию, поэтому **выдумывать факты нельзя**.
Источники, на которые можно опираться:

- `compani/about.html` — год основания (2005), номер лицензии, срок её действия,
  перечень услуг по 427-ФЗ, штат порядка 100 человек;
- `compani/price.html` — цены (дневной и ночной пост от 250 ₽/час, круглосуточный
  от 200 ₽/час, мероприятия от 400 ₽/час);
- `compani/services.html` — вооружение (ИЖ-71), удостоверение 6 разряда, порядок работы;
- `alt` благодарственных писем в `letters/` — список реальных клиентов.

Чего делать нельзя, с примерами из ревью PR #14 (всё это было снято до мержа):

- приписывать опыт, которого письма не подтверждают — письмо «Газпром инвеста» про
  охрану здания управления не даёт права заявлять опыт охраны стройплощадок;
- трактовать благодарность, если в ней не сказано, за что она — «Верю в чудо»
  поблагодарил, но не написал за охрану мероприятия;
- расширять единичный факт до множественного — один магазин «Светофор» не равен
  «магазинам сети»;
- переводить прошлое в настоящее — письма 2019 года не значат, что сотрудничество идёт;
- называть услуги, которых нет в прайсе (сопровождение инкассации);
- усиливать формулировки источника — «всегда на связи» не равно «на связи круглосуточно»;
- называть города области, если подтверждена только формулировка «Калининград и область».

## Как проверять

Доступ к API Яндекс.Вебмастера настроен. Токен лежит **вне репозитория**:
`C:\Users\user\.secrets\yandex-webmaster.token`, права `webmaster:hostinfo` +
`webmaster:verify`, действует 6 месяцев с 09.09.2026. В репозиторий он попасть не должен.

Постоянные значения:

- `user_id` — `1934888402`
- `host_id` — `https:www.bayard39.ru:443`
- база — `https://api.webmaster.yandex.net/v4/user/1934888402/hosts/https:www.bayard39.ru:443`

```bash
T=$(tr -d ' \r\n' < ~/.secrets/yandex-webmaster.token)
B="https://api.webmaster.yandex.net/v4/user/1934888402/hosts/https:www.bayard39.ru:443"

curl -s -H "Authorization: OAuth $T" "$B/summary"                      # страницы в поиске, ИКС
curl -s -H "Authorization: OAuth $T" "$B/diagnostics"                  # проблемы сайта
curl -s -H "Authorization: OAuth $T" "$B/search-urls/in-search/samples" # что реально в поиске
curl -s -H "Authorization: OAuth $T" "$B/indexing/samples?limit=50"     # коды ответа при обходе
curl -s -H "Authorization: OAuth $T" "$B/links/external/samples"        # внешние ссылки
```

Запросы и позиции — скриптом `docs/seo_diff.py`. Он тянет неделю из API, печатает
её рядом с предыдущей и дописывает в `docs/seo-history.json`:

```bash
python docs/seo_diff.py
```

`--dry-run` — только показать, `--selftest` — проверить логику дифа без сети.
Токен берётся из `YANDEX_WEBMASTER_TOKEN` или из `~/.secrets/yandex-webmaster.token`.

Историю коммитим: тогда тренд читается из `git log -p docs/seo-history.json`, а
отдельная база не нужна. Первая запись (30.08–05.09) занесена руками из таблицы
выше, дальше только API.

**«нет показов» в дифе — это не падение.** Запрос просто не набрал показов за
неделю, позиции по нему у Яндекса нет. При 9 показах в неделю так будет постоянно.

Сырой запрос, если нужен JSON:

```bash
curl -s -H "Authorization: OAuth $T" \
  "$B/search-queries/popular?order_by=TOTAL_SHOWS&query_indicator=TOTAL_SHOWS&query_indicator=TOTAL_CLICKS&query_indicator=AVG_SHOW_POSITION&limit=40"
```

Отправить страницу на переобход (суточная квота около 150 адресов):

```bash
curl -s -X POST -H "Authorization: OAuth $T" -H "Content-Type: application/json" \
  -d '{"url":"https://www.bayard39.ru/compani/ohrana-ofisa.html"}' "$B/recrawl/queue"
```

Эндпоинта `search-urls/excluded/samples` не существует — возвращает `RESOURCE_NOT_FOUND`.

**Google так не автоматизируется.** Indexing API принимает только страницы с разметкой
`JobPosting` или `BroadcastEvent`, для обычных страниц кнопка «Запросить индексирование»
в Search Console остаётся ручной. Читать статус можно через Search Console API, но он
пока не подключён.

## Сроки проверки результата

| Когда | Что смотреть | Статус |
|---|---|---|
| 3–4 дня после мержа | Попали ли новые страницы в индекс Яндекса (`search-urls/in-search/samples`) | ✅ 17.09: все 11 в поиске |
| 2–3 недели | Первые позиции по длинным запросам вроде «охрана склада калининград» | идёт, показов пока нет |
| 1,5–2 месяца | Сдвинулись ли основные запросы с 25–31 позиции | ждём, ориентир — конец октября |
| месяц и больше | Индексация новых страниц в Google | ждём |

## Что осталось, по убыванию отдачи

1. **Карточка в Яндекс.Бизнесе.** Закрывает `NOT_IN_SPRAV`, даёт показы в Картах и
   первое законное упоминание сайта. Бренд с 46 места вытащили страницами, но
   коммерческие запросы без карточки и ИКС с третьей страницы не уйдут. Только вручную.
2. **Внешние упоминания.** Сейчас ноль. Бесплатные размещения (2ГИС, Zoon, отраслевые
   каталоги ЧОО) и ссылки от клиентов, чьи благодарственные письма уже лежат в `letters/`.
3. **Разметка `JobPosting` на `vacan.html`.** Вакансии там реальные, разметка легально
   откроет для этой страницы Google Indexing API и показ в вакансиях Google. Единственная
   страница сайта, для которой автоиндексация в Google вообще возможна.
4. Счётчик посещаемости. От Яндекс.Метрики отказались 09.09.2026 — вместе с ней отпал
   и возврат политики обработки персональных данных, которой Метрика требует по 152-ФЗ.
   Пока источник данных о трафике один: «Эффективность» в Search Console, то есть только
   клики из Google.
